//import static org.junit.jupiter.api.Assertions.*;

import java.io.FileNotFoundException;
import java.util.ArrayList;

//import static org.junit.Assert.*;

// Didn't have enough time to do proper testing

public class JUnitTest {
    // GUIGame gui;
    // Model model;
    // Controller controller;
    ArrayList<String> list;

    /*
     * @Before
     * public void startGame() {
     * // model = new Model();
     * // model.initialise();
     * // start the game so there is something to test
     * }
     */

    // @Test
    public void ScannerTest() throws FileNotFoundException {
        // First test
        // assertEquals(model.get.....(), "");

        // Test if it found the questions / that scanner works to get text from file

        // nextQuestion
        // assertEquals question to file
    }

    // @Test
    public void PlayerScoreTest() throws FileNotFoundException {
        // Second test

        // Test if the player score gets updated when it should
        // Give player points, assert if total player score is equal to score + new
        // points
    }

    // @Test
    public void TeamScoreTest() throws FileNotFoundException {
        // Third test

        // Total team score is equal to player scores added together
        // assertEqual(Total score, (player score + player score +...))

    }
}